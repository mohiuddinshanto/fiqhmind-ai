# Backend API base URL. Either the full /api/v1 URL or a bare origin works:
# the app appends the /api/v1 prefix automatically when it is missing.
NEXT_PUBLIC_API_URL=https://fiqhmind-ai-backend.onrender.com/api/v1
